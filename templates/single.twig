{% extends "base.twig" %}

{% block content %}

	<div class="{{ post.post_type }}">

		<h1 class="post__title">{{ post.title }}</h1>
		<div class="post__content">
			{{post.content}}
		</div>

	</div>

{% endblock %}
